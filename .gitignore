# secrets — never commit
.env
.env.*
!.env.example
*.key
*.pem
*credentials*
!*credentials.example

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/

# tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# local data — the lake lives on the NAS, never in git
data/
*.duckdb
*.sqlite
*.sqlite3
*.db
*.parquet

# notebooks
.ipynb_checkpoints/

# serving replica — derived data, rebuilt from processed/
serving/
*.building

# os
.DS_Store
